## 1. 一次性机制建设

- [x] 1.1 创建持久化主题地图文件，列出跨境电商知识库应长期覆盖的主题范围、优先级、分类和目标笔记路径。
- [x] 1.2 创建知识清单生成方式，能够扫描 `cross-border-ecommerce/` 下所有 Markdown 文件并输出路径、标题、标题层级、修改时间、主题编号、分类和文件类型。
- [x] 1.3 创建过时内容识别规则，覆盖平台规则、费用、税务、合规、广告、物流、市场政策和工具推荐等时效性内容。
- [x] 1.4 创建刷新报告模板，用于记录每次运行的扫描结果、新增内容、更新内容、来源依据、延后项和验证结果。

## 2. 长期重复执行 Runbook

- [x] 2.1 编写 `cross-border-ecommerce` 知识刷新 runbook，说明每次执行时的固定步骤：扫描清单、对比主题地图、识别缺口、识别过时内容、查验来源、更新笔记、生成报告、运行构建验证。
- [x] 2.2 在 runbook 中定义默认刷新范围：每次优先处理少量高价值缺失主题和高风险过时主题，避免一次性重写大量笔记。
- [x] 2.3 在 runbook 中定义来源验证规则：稳定概念可基于本地上下文整理，变化较快的事实必须查验当前来源后再更新。
- [x] 2.4 在 runbook 中定义完成标准：刷新报告已生成、变更文件已列出、延后项已记录、`mkdocs build --strict` 已通过。

## 3. 首次运行验证

- [x] 3.1 按新 runbook 执行一次首轮知识清单扫描，并生成首份刷新报告。
- [x] 3.2 对比主题地图与当前清单，列出首批缺失主题和过时风险主题。
- [x] 3.3 选择一小批高优先级主题进行补充或更新，保留现有目录结构和已有有效内容。
- [x] 3.4 对本次更新涉及的时效性事实进行来源查验，并在报告或笔记中记录依据。

## 4. 文档入口与可发现性

- [x] 4.1 根据首轮新增或更新内容，同步更新必要的总览、进度或索引文件。
- [x] 4.2 确认新增主题能通过 MkDocs 左侧目录或知识库入口页被发现。
- [x] 4.3 保持现有文件夹名称和 MkDocs 兼容的 Markdown 结构，除非必须修复具体路径问题。

## 5. 验证与交付

- [x] 5.1 在项目根目录运行 `mkdocs build --strict`。
- [x] 5.2 修复构建发现的编码、路径、导航或 Markdown 问题。
- [x] 5.3 确认刷新机制相关文件、首轮刷新报告、内容变更和构建验证结果都已记录。
- [x] 5.4 在最终说明中写清楚：后续重复执行不再重新完成本 tasks 文件，而是按 runbook 周期性运行。
